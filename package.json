{
  "name": "fin-bridge",
  "version": "0.2.0",
  "description": "This project is an attempt to simulate Open Finance and put into practice some concepts that were recently studied.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "start:dev": "tsx --env-file=.env src/server.ts",
    "start:watch": "tsx watch --env-file=.env src/server.ts",
    "dist": "tsup src",
    "start:dist": "npm run dist && --env-file=.env node/serves.js"
  },
  "keywords": [],
  "author": "bacate",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
